const initialState = {
  portfolio: [
    {
      amount: "1",
    token:"betacoin",
    costbasis: "5",
    wallet: "Betacoinbase"}
  ]
}

export default function manageCrypto(state = initialState, action){

  switch(action.type){


    default:
      return state
  }
}
